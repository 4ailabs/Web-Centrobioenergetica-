import { prisma } from '../lib/prisma.js'; import bcrypt from 'bcryptjs'; async function main() { const hashedPassword = await bcrypt.hash('Admin123!', 10); await prisma.user.update({ where: { email: 'admin@instituto.com' }, data: { password: hashedPassword, approved: true, isAdmin: true } }); console.log('✅ Password for admin@instituto.com has been reset to: Admin123!'); } main().finally(() => prisma.$disconnect());
